{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "Secret": "${JWT_SECRET}",
    "Issuer": "BookGuess",
    "Audience": "BookGuess"
  },
  "ConnectionStrings": {
    "DefaultConnection": "${MYSQL_CONNECTION}"
  },
  "AIService": {
    "BaseUrl": "${AISERVICE_URL}"
  },
  "Cors": {
    "AllowedOrigins": "${CORS_ORIGINS}"
  }
}
